CREATE TABLE meteorite_landings(

    name VARCHAR(30),
    id INT PRIMARY KEY,
    nametype varchar(30),
    recclass varchar(30),
    mass FLOAT,
    fall VARCHAR(10),
    year DATE,
    reclat FLOAT,
    reclong FLOAT
);